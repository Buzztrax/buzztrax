Read the following sections:

    PATT
    WAVE
    PDLG (will probably be ignored forever)
    MIDI

Uncompress CWAV audio data.
Therefore port over lines 130-480 from decomp.cpp
1.) replace ->hFile and
      SetFilePointer
			ReadFile

